version: "3.8"

services:
  app:
    image: node:18
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development

    command: tail -f /dev/null
